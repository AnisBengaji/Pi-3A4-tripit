/*package org.projeti.Controlleurs;
import javafx.fxml.FXML;
import javafx.scene.control.Button;
import javafx.scene.control.TextField;
import javafx.scene.image.ImageView;
import javafx.scene.layout.VBox;

public class trippin  {

    @FXML
    private TextField searchField;

    @FXML
    private VBox menuBox;

    @FXML
    private Button homeButton, allTripsButton, travelsButton, roomsButton, transportButton, eventsButton, offersButton;

    @FXML
    private ImageView romeImage, londonImage, europeImage, promoImage;

    @FXML
    private void initialize() {
        // Exemple d'initialisation des événements
        homeButton.setOnAction(event -> navigate("Home"));
        allTripsButton.setOnAction(event -> navigate("All Trips"));
        travelsButton.setOnAction(event -> navigate("Travels"));
        roomsButton.setOnAction(event -> navigate("Rooms"));
        transportButton.setOnAction(event -> navigate("Transport"));
        eventsButton.setOnAction(event -> navigate("Events"));
        offersButton.setOnAction(event -> navigate("Offers"));
    }

    private void navigate(String section) {
        System.out.println("Navigating to: " + section);
        // Implémentation de navigation
    }
}
}
*/